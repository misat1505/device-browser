import { DeviceBrowserPage } from "./pages/DeviceBrowserPage";

// here there would be a router configuration with react-router-dom
// or with tanstack router

// in this example we have 1 page so there is no need for it

function App() {
  return (
    <main>
      <DeviceBrowserPage />
    </main>
  );
}

export default App;
